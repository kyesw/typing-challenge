apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: typing-game

commonLabels:
  app: typing-game-backend

resources:
  - deployment.yaml
  - service.yaml
  - configmap.yaml
  - secret.yaml
  - ingress.yaml

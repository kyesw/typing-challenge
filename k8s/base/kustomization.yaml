apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: typing-game

labels:
  - pairs:
      app: typing-game-backend
    includeSelectors: true

resources:
  - deployment.yaml
  - service.yaml
  - configmap.yaml
  - ingress.yaml
